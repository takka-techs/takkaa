# تفاصيل مشروع Takka POS & ERP 📱💼

هذا الملف (Markdown) يُلخص بالتفصيل كل ما تم إنجازه وبناؤه في مشروع **Takka POS & ERP** (نظام محاسبة متكامل لإدارة محلات الموبايلات والصيانة).

---

## 🌟 نظرة عامة على المشروع (Project Overview)
نظام **Takka POS & ERP** هو تطبيق ويب حديث ومتكامل يعمل كنقطة بيع (POS) ونظام تخطيط موارد المؤسسات (ERP). صُمم خصيصاً لإدارة محلات بيع الهواتف الذكية، والإكسسوارات، وقطع الغيار، إلى جانب إدارة ورش وقسم الصيانة، والمخازن، والتقسيط، والحسابات المالية والرواتب.

---

## 🛠️ التقنيات والمكتبات المستخدمة (Tech Stack)
تم بناء المشروع باستخدام أحدث تقنيات تطوير واجهات الويب لدعم الأداء وتجربة المستخدم الممتازة:

* **بيئة التشغيل والبناء والإطار العام**: React 19 مع بيئة Vite العصرية باستخدام لغة TypeScript (`tsx`).
* **التصميم وواجهة المستخدم (UI/UX)**: 
  * إطار CSS: Tailwind CSS إصدار 4 (للتحكم الدقيق والسريع في التصميم والتجاوب).
  * الأيقونات: مكتبة `lucide-react`.
  * الحركات والأنميشن: `motion/react` لضمان تجربة مستخدم سلسة مثل الإنتقالات والانزلاقات.
* **البنية التحتية وقواعد البيانات (Backend/DB)**: تم الاعتماد على **Supabase** (التي تعتمد على PostgreSQL) لإدارة قواعد البيانات والتوثيق، مع وجود سكريبتات SQL مكثفة لبناء وصيانة هيكل الجداول.
* **الرسومات البيانية والتحليل**: `recharts` لتوليد الرسوم البيانية في لوحة التحكم وتقارير التدفق النقدي.
* **التعامل مع التواريخ**: مكتبة `date-fns` مع دعم التوطين العربي (ar).
* **إدارة الطباعة والمستندات**:
  * طباعة التقارير: `react-to-print`.
  * تصدير PDF/الصور: `html2pdf.js` و `jspdf` و `html-to-image` و `html2canvas`.
  * الباركود و QR Code: `react-barcode` و `react-qr-code`.
  * التعامل مع ملفات Excel: `xlsx`.

---

## 🗂️ هيكل النظام والوحدات البرمجية (System Modules)

النظام مقسم إلى وحدات رئيسية (Modules) تظهر في لوحة التحكم الجانبية (Sidebar) بصلاحيات مخصصة لكل موظف:

### 1. 🔐 نظام تسجيل الدخول والصلاحيات (Auth & Roles)
* **دخول الإدارة (المالك)**: يمتلك صلاحيات مطلقة على النظام.
* **دخول الموظفين / الكاشير**: يتم التحكم بالميزات التي يراها الموظف بناءً على الصلاحيات المخصصة له من الإدارة.
* **ربط الأجهزة**: لا يمكن للكاشير الدخول إلا إذا تم تسجيل الدخول المسبق من المالك لربط الجهاز بالنظام كتدبير أمني.

### 2. 🏠 الرئيسية (Dashboard)
لوحة تحكم تفاعلية تحوي:
* ملخصات المبيعات والمشتريات.
* الرسوم البيانية للأرباح والإيرادات.
* إشعارات النواقص في المخزون وحالات الصيانة الحالية.

### 3. 🛒 أدوات البيع المباشر (POS)
* شاشة نقطة البيع السريعة (POS) للمبيعات المباشرة.
* التكامل مع أجهزة قراءة الباركود، وإنشاء الفواتير للعملاء وطباعتها فورياً.

### 4. 🪛 قسم الصيانة (Maintenance)
إدارة ورش الصيانة وطلبات تصليح الأجهزة، وتتضمن:
* استلام الأجهزة من العملاء وإصدار إيصالات الصيانة وملصقات الجهاز.
* متابعة حالات الأجهزة (في الانتظار، جاري العمل المرفوضة، مُكتملة).

### 5. 📦 إدارة المخزون والمخازن (Warehouses & Inventory)
تم فصل المخازن إلى أقسام رئيسية لسهولة التتبع:
* **الأجهزة (Devices)**: تتبع الهواتف الذكية مع نظام صارم للسيريال نمبر (IMEI).
* **الإكسسوارات (Accessories)**: متابعة الملحقات بكمياتها وأسعارها.
* **قطع الغيار (Spare Parts)**: تتبع دقيق للقطع المستخدمة بالصيانة أو المباعة.
* **جرد المخازن (Inventory)**: نظام تسوية للجرد وإشعارات وتنبيهات مستمرة للمخزون المنخفض (Low Stock Report).

### 6. 💰 العمليات التجارية (المبيعات والمشتريات)
* **المبيعات (Sales)**: تنقسم إلى مبيعات عامة، أجهزة، إكسسوارات، وقطع غيار.
* **المشتريات (Purchases)**: إدارة فواتير المشتريات من الموردين لكل نوع من أنواع المخزون على حدة.

### 7. ⚖️ نظام التقسيط (Installments)
نظام متكامل خاص بأقساط الأجهزة ويتضمن:
* **لوحة تحكم التقسيط (Dashboard)** خاصة به.
* **عقود التقسيط (Contracts)**.
* **توقعات التدفق النقدي (Cash Flow Forecast)** و **سجل الحركات (Audit Logs)** لضمان تتبع المستحقات بدقة.

### 8. 💸 الحسابات والمالية (Accounting & Finance)
* **الخزينة (Treasury)**: تتبع الحركة اليومية للأموال الصادرة والواردة داخل الدرج/الخزينة.
* **الحسابات العامة (General Accounts)**: تسجيل القيود المحاسبية.
* تقارير مخصصة للإيرادات والمصروفات، وقائمة المركز المالي بشكل مبسط.

### 9. 👥 إدارة علاقات العملاء والموردين (CRM / Partners)
* قوائم لتسجيل **العملاء (Customers)** وبياناتهم.
* قوائم **الموردين (Suppliers)** لتتبع المدفوعات.
* **الشركاء (Partners)** للتعامل مع الشركاء بالحصص في المشروع.

### 10. 🧑‍💼 الإدارة وشؤون الموظفين (HR)
* **الموظفين (Employees)**: إضافة الموظفين وتسجيل الإجازات والحضور والانصراف.
* **الرواتب (Salaries)**: إدارة صرف مرتبات الموظفين والخصومات.
* **المستخدمين (Users)**: إدارة حسابات النظام والصلاحيات.

### 11. 🧠 أدوات مساعدة و ذكية (Smart Tools)
* **مُتتبع السيريال (IMEI Tracker)**: تتبع تفصيلي لأي جهاز من وقت شرائه وحتى بيعه وكل العمليات التي طرأت عليه.
* **القائمة السوداء (Blacklist)**: لمنع التعامل مع عملاء معينين.
* **التذكيرات (Reminders)** والإشعارات.
* **الأرشيف (Archive)** للبيانات القديمة والمرتجعات وغيرها، و **دليل الاستخدام (Manual)**.

---

## 🎨 واجهة المستخدم وتجربة المستخدم (UI & UX Features)

* **الوضع المظلم والفاتح (Dark/Light Mode)**: يتيح النظام التبديل السريع بين الوضعين لراحة العين.
* **دعم الألوان (Theming)**: إمكانية اختيار اللون الأساسي (Teal، Blue، Purple، Emerald، إلخ) ليتلائم مع الهوية البصرية للمشروع.
* **ملء الشاشة (Fullscreen)**: توفير مساحة كاملة لاستخدام لوحة التحكم.
* **تصميم متجاوب**: تم بناء التصميم ليكون داعماً لجميع أحجام الشاشات باستخدام Tailwind Mobile-First.
* **زر عائم الدعم المباشر**: للتواصل الفوري مع الدعم الفني عبر WhatsApp من أي صفحة.

---

## 📁 الهيكل البرمجي لملفات المشروع بداخل `src/`

تم ترتيب المنطق البرمجي والملفات بعناية في مجلد الـ Source:

* `src/components/`: يحتوي تقريباً على كل واجهات النظام، من قوائم، موديلات (Modals)، وعناصر واجهة مفردة.
* `src/pages/`: (غير مستخدم بكثرة نظراً لأن أغلب النظام مبني كـ Single-Page Application (SPA) مكون من Components داخل App.tsx).
* `src/contexts/`: يستخدم الـ React Context Management لإدارة (Settings و غيره) على مستوى التطبيق بالكامل.
* `src/lib/` و `src/utils/` و `src/constants/`: مسارات للتوابع والمُساعدات البرمجية والأدوات التي تعزز تجربة المطور وتمنع التكرار (DRY).
* `src/services/` و `src/types/`: تعريف أنواع البيانات والمجسمات (TypeScript Types)، وخدمات الربط مع الواجهات البرمجية.

---

## ⚙️ التحديثات ومعالجة قواعد البيانات
يلاحظ في المجلد الرئيسي (Root) وجود العديد من ملفات `Migration` التابعة بقاعدة بيانات PostgreSQL مثل:
* `final_deployment.sql`, `v2_fixes_deployment.sql`, `prod_upgrade.sql`, وغيرها من ملفات الـ `SQL`.
وهذا يدل على وجود استمرارية وعناية بهيكل الجداول، وإصلاح قيود (Foreign Keys)، وكتابة توابع قاعدة بيانات (RPC Triggers) لضمان اتساق البيانات وسلامة النظام.

---

**تم الانتهاء من الملف، وهو الآن يعكس كامل بنية وتفاصيل سير العمل، وإدارة المشروع من الصفر وحتى الشكل النهائي المكتمل.**
