# تقرير وتقييم لمشروع نظام الفروع ونقاط البيع (POS / ERP)

## 1. نظرة عامة على المشروع (Overview)
النظام عبارة عن تطبيق ويب متكامل لإدارة الأعمال ومحلات الصيانة والهواتف، وهو بمثابة **برنامج لتخطيط موارد المؤسسات (ERP)** مصغر.
- **الواجهة الأمامية**: مبنية باستخدام `React` و `TypeScript` و `Tailwind CSS`.
- **قاعدة البيانات**: `Supabase` وتستخدم دوال وجداول مرتبطة بالـ (REST/RESTful APIs) لإدارة البيانات.
- **نقاط القوة**: يدعم النظام **تعدد الفروع (Multi-branch)** و**المالكين المتنوعين (Multi-tenancy)**، للتحكم في المخازن ونقاط البيع الخاصة بكل فرد دون تداخل بياناتهم.

---

## 2. ماذا تم إنجازه مؤخراً؟
خلال العمليات والتحديثات الأخيرة تم إصلاح وتعديل الآتي:

1. **تصحيح عرض المحافظ/الخزائن في صرف الرواتب**:
   كان النظام يُظهر كافة الخزائن المتواجدة في التطبيق لأي موظف مما يؤدي لدفع رواتب من حسابات لا يملكها. 
   تمت البرمجة ليتم فلترة الخزائن بناءً على الـ `tenant_id` (صاحب المتجر).
   *كما تم إضافة شرط أمان*: لو كان المستخدم (مدير فرع)، فسيظهر له فقط خزنة "الكاش" التابعة لفرعه. ولو كان مالك "Owner"، ستظهر له جميع الخزن الخاصة بحسابه (الرئيسية والفرعية).

2. **تحسين تقارير أداء الفروع (Branch Analytics)**:
   تم تعديل الحسبة الخاصة بمبيعات الفروع وأرباحهم وقيم الصيانة لتستند إلى الأسماء والتفاصيل الدقيقة للأعمدة بقاعدة البيانات مثل (`net_amount`، `total_amount`، و `receiving_branch_id`).

3. **الفصل الآمن (Tenancy Separation)**:
   تم إصلاح العديد من شاشات المالية (Treasury، Cashflow، Partners) لتستخدم المتغيرين الصحيحين: إما التصفية بناءً على مبيعات الفرع النشط `branch_id` أو التصفية للمتجر بأكمله `tenant_id` لضمان عدم حدوث تداخلات أو خلل في الأرصدة.

---

## 3. ملخص الميزات والموديلات المكتملة والشغالة بالمشروع (Features):
- **نقاط البيع (POS)**: عمليات بيع وإرجاع (أجهزة، قطع غيار، إكسسوارات) وطباعة فواتير حرارية باللمس.
- **الصيانة (Maintenance)**: تسجيل أجهزة صيانة، تسلميها وتسليمها لفروع أخرى عبر "الصيانة المركزية"، تكلفة وفحص الجودة وإيصالات الاستلام.
- **الموارد البشرية (HR & Payroll)**: إدارة موظفين كاملة، رواتب، سلفيات قروض شهرية، ونظام الحضور والانصراف، وطلبات الإجازات.
- **التدفق النقدي والخزن (Finance / Treasury)**: إضافة أصول وإيداعات وحركات مصروفات، وقيود تحويل بين الفروع وبين خزن الكي فلو/البنك. وتقارير الأرباح والميزانية (`PnL`, `Cashflow`, `Capital`).
- **المشتريات وإدارة المخازن (Inventory & Warehouses)**: شاشات لإدارة مخزون البضاعة وجردها، تحويل الأصناف بين المخازن وسجل الموردين.
- **الأقساط (Installments)**: إدارة العقود والمدفوعات المتأخرة وتسديدها.

---

## 4. قائمة بالأخطاء والبجات (Bugs & Flaws) التي تم رصدها وتحتاج إلى انتباه:

1. **مشكلة المتغير (owner_id) القديم**:
   - الكثير من الجداول (مثل `employees`، `employee_loans`، `attendance`) في كود الرياكت ما زالت تعتمد على الاستعلام `owner_id=eq.${userId}` بدلاً من المتغير الأدق `tenant_id`. 
   - *التأثير:* في المستقبل عند نقل قاعدة البيانات أو تطوير الأدوار، قد يؤدي هذا لاختفاء بعض بيانات الموظفين ويجب إطلاق حملة تصليح (Find and Replace) لتوحيدها إلى `tenant_id`.
   
2. **عدم وجود نظام جلب على شكل صفحات (No Pagination)**:
   - الكود يسجل الجلب بأوامر مثل: `fetch(/rest/v1/treasury_transactions)` دون استخدام `limit` أو `offset`.
   - *التأثير:* إذا كان لدى المتجر المئات من المبيعات والحركات، سيصبح فتح بعض الصفحات ثقيلاً جدًا وسيواجه المتصفح تقطيعاً. يرجى تطبيق `Pagination` أو التمرير اللانهائي (Infinite Scroll).

3. **الأمان وصلاحيات الواجهة (Frontend Only Security)**:
   - في بعض الأحيان يتم إخفاء أزرار التعديل والحذف لك لأنك "لست مالكاً" عبر المتغير `isOwner` في React. هذا يعني أن "واجهة المستخدم" هي التي تحميك. ولكن تقنياً لو استخدم الموظف (Postman) أو الـ Console سيمكنه إجراء تعديل مسروق لو لم يتم تطبيق قواعد الأمان الصارمة من جهات السيرفر (Row Level Security في Supabase). يجب مراجعة الـ RLS والتأكد أن لا أحد يستطيع حذف سجل غير مملوك له.

4. **توقيع أخطاء في عرض محافظ بعض الصفحات**:
   - لا تزال صفحة (`Maintenance.tsx` بالسطر 2986 والسطر 2993) تجلب محافظ الكاش باستخدام `owner_id`. من الممكن أن يواجه بعض المستخدمين مشكلة عند تسديد مبالغ الصيانة، ويجب تصليحها لتمشي مع `tenant_id`.

5. **أرصدة تقرير الـ Cashflow الدائرية**:
   - حساب الأرباح التراكمية الدورية في تقرير رأس المال، أحياناً يجلب عمليات الخزينة الخاصة بفرع، ولكنه يخلط أرصدة البنوك لكافة الفروع لعدم وجود فصل قوي.

---

## 5. مقتراحات التطوير والخطوات القادمة (Next Steps / Roadmap)

1. **إجراء عملية بحث وإصلاح ضخمة (Refactoring)** لتبديل أي كود خاطئ يستخدم `owner_id=eq` وتحويله ليتوافق مع المعايير الحالية `tenant_id=eq`. المتبقي منه: قسم (الشركاء Partners)، الموارد البشرية، والصيانة.
2. **برمجة صفحة التقارير واللوحات البيانية (Charts)** لتصبح أكثر تفاعلية باستخدام مكتبة الرسومات لتوفر للمالك نظرة بانورامية لجميع فروعه وطلبه.
3. التفكير في **الطباعة المباشرة** وتحديث تصميم الفواتير الخاص بكسر حجم الصفحة المزعج في بعض طابعات الباركود (Thermal Printers 80mm).
4. تحسينات الأداء بتقنيات (Lazy Loading & Debouncing) في مربع البحث داخل نقطة البيع (POS).

---
*(هذا التقرير متاح بين يديك في الملف `PROJECT_REPORT.md`)*
